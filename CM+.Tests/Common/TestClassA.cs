class TestClassA
{
    /// <summary>
    /// The test1
    /// </summary>
    int test1;

    /// <summary>
    /// The test2
    /// </summary>

    int test2;

    void Test()
    {
        System.Console.WriteLine("");
        var ttt = ""
       .Select(x => x)

   .Select(x => x)
   .Select(x => x)
         .Select(x => x)
         .Select(x => x);

        if (true)
        {
            var ttt2 = ""
      .Select(x => x)
   // dfs
   .Select(x => x)
   .Select(x => x)
 .Select(x => x)
     .Select(x => x);
        }

        Console.WriteLine(""
       .Select(x => x));
    }
}